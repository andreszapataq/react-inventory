import { Switch } from "react-router-dom" // REACT ROUTER OR DOM?

const Routes = () => {
    return (
        <div>
            <Switch>
                
            </Switch>
        </div>
    )
}

export default Routes
